export enum DataRefBehavior {
    REQUIRED = 'required',
    IMMEDIATE = 'immediate',
    OPTIONAL = 'optional',
    VISIBLE = 'visible',
    EDITABLE = 'editable',
    HIDDEN = 'hidden',
    FORBIDDEN = 'forbidden'
}
